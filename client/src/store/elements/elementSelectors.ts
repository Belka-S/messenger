export const selectElements = (state: any) => state.elements.items;
export const selectElementFilter = (state: any) => state.elements.filter;

export const selectIsLoading = (state: any) => state.elements.isLoading;
export const selectError = (state: any) => state.elements.error;
